export enum Selectors {
    StartButton = ".start-button",
    RestartButton = ".restart-button",
    Title = ".title",
    Task = ".task-wrapper",
    ColorName = ".color-name",
    Canvas = "canvas",
    ScoreWrapper = ".score-wrapper",
    Score = ".score-text",
}

export enum SceneSelectors {
    IntroScene = ".scene-intro",
    GameScene = ".scene-game",
    EndScene = ".scene-end",
}